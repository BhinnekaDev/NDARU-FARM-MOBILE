import { TextStyle } from "react-native";

export interface buttonBackProps {
  onPress?: () => void;
  myClassName?: string;
  mySize?: number;
  myColor?: string;
  myActiveOpacity?: number;
  iconStyle?: TextStyle;
  }
